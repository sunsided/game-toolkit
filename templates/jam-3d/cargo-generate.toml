[template]
description = "A 3D game-jam starter built on game-toolkit (spinning cube + 2D HUD)"
# Copy the Taskfile verbatim: its go-task `{{ }}` would clash with the template engine.
exclude = ["Taskfile.yaml"]
